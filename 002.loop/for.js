/*
let x = 0;
let y = 0;

for (x; x < 2; x++) {
  console.log("---x-------->", x, y);
  for (y; y < 2; y++) {
    console.log("----y--->", x, y);
  }
}


/*
x => 0 => t => y => 0 => t => (0,0) => y++ => y => 1 => (0,1) => 
x => 1 => t => 
*/

for (let x = 0; x < 2; x++) {
  for (let y = 0; y < 2; y++) {
    console.log("🚀 ~ --->", x, y);
  }
}


/*
 x => 0 => t => y => 0 => t => (0,0) => y => 1 => t => (0,1) 
 x => 1 => t => y => 0 => t => (1,0) => y => 1 => t => (1,2)
 x => 2 => f
*/
